DACOMP, stand-alone simple compiler for educative pourposes. 
Uses Flex/Bison.

Assumes DAVM is installed in the parent directory.

Developed by:
- Arturo San Emeterio Campos (arturocampos82@gmail.com)
- Daniel Peyrolón 

Some scripts are provided to ease use:
1) clean_install, install at current dir /install
(also create /build but both are at gitignore)
2) execute_tests, run the acceptance tests in /install
3) regenerate_tests, use the current output of the tests as the proper
base line. WARNING: this is written to /src